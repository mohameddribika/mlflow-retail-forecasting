# Core ML stack
scikit-learn==1.5.2
pandas==2.2.3
numpy==1.26.4
xgboost==2.1.3

# MLflow lifecycle management
mlflow==2.18.0

# Hyperparameter tuning
hyperopt==0.2.7

# Visualization (for EDA + report figures)
matplotlib==3.9.2
seaborn==0.13.2

# Notebook environment for EDA
jupyter==1.1.1
ipykernel==6.29.5

# Data drift detection for monitoring step
evidently==0.4.40

# Serving / API client utilities
requests==2.32.3

# Pretty CLI output
rich==13.9.4

# Kaggle dataset downloader.
# kagglehub supports the new KAGGLE_API_TOKEN env var (KGAT_xxx tokens).
# kaggle (legacy) supports the old kaggle.json file. We install both for
# flexibility — the loader prefers kagglehub if the new token is present.
kagglehub==0.3.4
kaggle==1.6.17
